name: CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  status:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Repository status
        run: |
          echo "MELISA / repo CI is healthy"
          echo "Repo: ${{ github.repository }}"
          echo "Ref:  ${{ github.ref }}"